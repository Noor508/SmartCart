<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DevilApplication"
        tools:targetApi="31">


        <activity
            android:name=".Activities.RegistrationActivity"
            android:exported="true" />
        <activity
            android:name=".Activities.HomeActivity"
            android:exported="true" /> <!-- <activity -->
        <activity
            android:name=".Activities.SplashScreen"
            android:exported="true"
            android:label="Devil&apos;s App">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.LoginActivity"
            android:exported="true" />
        <activity
            android:name=".Activities.SignupActivity"
            android:exported="true" />
        <activity
            android:name=".Activities.CalculatorActivity"
            android:exported="true" /> <!-- <activity -->
        <activity
            android:name=".Room.CrudActivity"
            android:exported="true" />
        <activity
            android:name=".Activities.UpdateActivity"
            android:exported="true" />
        <activity android:name=".API.GET.Display_API" />
        <activity android:name=".API.POST.Display_POST" />
        <activity android:name=".Fragments.DynamicFragment" />
        <activity android:name=".RecyclerView.Item_Product" />
        <activity android:name="com.example.myapplication.activities.SignupActivity" />

    </application>

</manifest>